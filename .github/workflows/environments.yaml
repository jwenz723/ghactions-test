name: "environments"

on:
  pull_request:
    branches:
      - main
#    paths:
#      - "environments/**"
  push:
    branches:
      - main
#    paths:
#      - "environments/**"

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: calculate diff
        run: git diff-tree --no-commit-id --name-only -r ${{ github.sha }}